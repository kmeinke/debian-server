mail:
  domain: example.com
  root_alias: admin@example.com
  # exim4 smarthost format (host::port)
  smarthost: mail.example.com::587
  # postfix smarthost format (separate host and port)
  smarthost_host: mail.example.com
  smarthost_port: 587
